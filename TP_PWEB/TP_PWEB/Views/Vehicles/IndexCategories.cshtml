@model HashSet<string>

@{
    ViewBag.Title = "IndexCategories";
}

<h2>Categories</h2>

<ul class="list-group">
    @foreach (var item in Model)
    {
        <li>
            @Html.DisplayFor(modelItem => item)
            @Html.CheckBox(item, true)
        </li>
    }
</ul>